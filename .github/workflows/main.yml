name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: dev

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Add SSH Key to known_hosts
      run: |
        echo "${{ secrets.VM_IP_ADDRESS }} $(ssh-keyscan -H ${{ secrets.VM_IP_ADDRESS }})" >> ~/.ssh/known_hosts
        
    - name: Setup SSH
      uses: webfactory/ssh-agent@v0.7.0
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    - name: SSH and Deploy
      run: |
        ssh ${{ secrets.VM_USERNAME }}@${{ secrets.VM_IP_ADDRESS }} << 'ENDSSH'
          cd /srv/full-stack-project-blueprint
          git pull origin main
          docker-compose -f /srv/full-stack-project-blueprint/src/backend/docker-compose.yml down
          docker-compose -f /srv/full-stack-project-blueprint/src/backend/docker-compose.yml up -d --build
        ENDSSH
